let 

let textValue = "";

function addNumero(number) {
    textValue += number
}

function addOperator(Operator) {
    textValue += Operator
}

function addDecimal() {
    textValue = "."
}

function clearText() {
    Input.textContent
}